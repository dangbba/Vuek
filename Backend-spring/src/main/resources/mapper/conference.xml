<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.ssafy.backend.model.mapper.ConferenceMapper">
 	<insert id="createConference" parameterType="ConferenceDto">
		insert into conference (idconference, conference_type_idconference_type, book_detail_idbook_detail, user_iduser, call_start_time, call_end_time, thumbnail_url, conference_title, is_active)
		values (#{idconference}, #{conference_type_idconference_type}, #{book_detail_idbook_detail}, #{user_iduser}, #{call_start_time}, #{call_end_time}, #{thumbnail_url}, #{conference_title}, #{is_active})
	</insert>
	
	<select id="getConferenceCategory" resultType="ConferenceTypeDto">
		select *
		from conference_type
	</select>
 
 	<select id="getConferenceInfo" parameterType="int" resultType="ConferenceDto">
		select *
		from conference
		where idconference = #{idconference}
	</select>
	
	<update id="updateConferenceInfo" parameterType="ConferenceDto">
		update conference set conference_type_idconference_type = #{conference_type_idconference_type}, book_detail_idbook_detail = #{book_detail_idbook_detail}, user_iduser = #{user_iduser}, thumbnail_url = #{thumbnail_url}, conference_title = #{conference_title}, is_active = #{is_active}
		where idconference = #{idconference}
	</update>
 </mapper>
